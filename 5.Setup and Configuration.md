## Setup and Configuration

### Installation Steps

**Installing Ansible on Control Node (Ubuntu):**

```bash
sudo apt update
sudo apt install ansible
```

**Check Ansible version:**
```bash
ansible --version
```
